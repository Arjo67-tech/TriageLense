# Butterbase Data API base for the triagelens app (includes app id).
# Public Data API only — no secret key is used in the browser.
NEXT_PUBLIC_BUTTERBASE_URL=https://api.butterbase.ai/v1/app_wm0lacszkes9
