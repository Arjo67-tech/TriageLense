# Copy to .env.local. Butterbase Data API base (includes the app id).
# This is a PUBLIC endpoint used with the anonymous role — never put a
# bb_sk_* secret key here; it would be exposed to the browser.
NEXT_PUBLIC_BUTTERBASE_URL=https://api.butterbase.ai/v1/<your_app_id>
